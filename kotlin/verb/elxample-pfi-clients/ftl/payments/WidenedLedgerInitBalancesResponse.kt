package ftl.payments

import kotlin.collections.List
import xyz.block.ftl.GeneratedRef

@GeneratedRef(
  name="WidenedLedgerInitBalancesResponse",
  module="payments",
)
public data class WidenedLedgerInitBalancesResponse(
  public val balances: List<WidenedLedgerInitBalance>,
)
